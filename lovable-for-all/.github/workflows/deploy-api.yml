name: Deploy API to Railway

on:
  push:
    branches: [main]
    paths:
      - "lovable-for-all/apps/api/**"
      - "lovable-for-all/packages/db/**"
      - "lovable-for-all/packages/shared/**"
      - "lovable-for-all/packages/ai-providers/**"

defaults:
  run:
    working-directory: lovable-for-all

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Typecheck
        run: bun run typecheck --filter=@lovable/api

      - name: Deploy to Railway
        uses: railwayapp/railway-deploy@v1
        with:
          service: lovable-api
          environment: production
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
